pipeline {
    agent { dockerfile true }

    stages {
        stage('Post Build Message') {
            
            post {
                success {
                    echo 'Image builing is complete'
                }
            }
        }
    }
}
